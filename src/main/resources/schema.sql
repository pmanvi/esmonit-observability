CREATE TABLE `product` (
                        `id` INT(11) NOT NULL,
                        `name` VARCHAR(500) NULL DEFAULT NULL,
                        `category` VARCHAR(500) NULL DEFAULT NULL,
                        PRIMARY KEY (`id`)
);

